export enum ExpressionNotation {
    PREFIX = 0,
    INFIX = 1,
    POSTFIX = 2,
}

export enum ExpressionContext {
    REAL = 0,
    BIG_INTEGER = 1,
    BINARY = 2,
    BOOLEAN = 3,
    MATRIX = 4,
    SET = 5,
    STRING = 6,
}